# apps/admin does not currently read any project-specific .env variables.
#
# Notes:
# - The admin web app uses same-origin requests to /api/* and /admin/*.
# - Route / port wiring is usually provided by the entry service or external reverse proxy.
