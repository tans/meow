# apps/web does not currently read any project-specific .env variables.
#
# Notes:
# - The web app uses same-origin requests to /auth/*, /creator/*, and /merchant/*.
# - Route / port wiring is usually provided by the entry service or external reverse proxy.
